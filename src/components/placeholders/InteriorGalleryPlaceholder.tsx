import { MediaPlaceholder } from "./MediaPlaceholder";

export function InteriorGalleryPlaceholder() {
  return (
    <MediaPlaceholder
      title="Interior Gallery Placeholder"
      description="Replace with high-resolution interior photography once final edits are complete."
    />
  );
}
