import { MediaPlaceholder } from "./MediaPlaceholder";

export function FloorplanPlaceholder() {
  return (
    <MediaPlaceholder
      title="Floorplan Placeholder"
      description="Replace this card with final, dimensioned floorplans in image or PDF format."
    />
  );
}
